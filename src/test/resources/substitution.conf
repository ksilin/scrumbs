db {

  protocol = "jdbc:mysql://"

  locations {
    localhost {
      port = 6603
      user = "root"
      password = "secret"
    }

    remote {
      port = 32817
      user = "exSqlUser"
      password = "Exellio"
    }
  }

  test_db {
    db = tecdoc
    host = localhost
  }

}